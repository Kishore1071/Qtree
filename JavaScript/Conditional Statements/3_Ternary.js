const definition = "Ternary operator can be used as a short hand for if statement or Switch Statement only when there is two possible outcomes"

let isRaining = true;
let weatherMessage = isRaining ? "Bring an umbrella." : "Enjoy the sunshine.";

console.log(weatherMessage);